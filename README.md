# CHAT

## Run server

* `cd chat-server`

* for dev: `npm run dev`
* start: `npm start`
* start docker: `npm run docker`

## Run client

* `cd chat-client` or * `cd chat-client-mobile`

* start: `npm start`